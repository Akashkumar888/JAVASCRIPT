
// Arrow Functions (let f = () => {})


let f = () => console.log("hi");


// Behaves like a let function expression (no hoisting).
// Does not have its own this, arguments, or prototype.
// Great for short callbacks, but cannot be used as constructors.
